<div class="container">
  <h2 class="mt-4">Files</h2>

  {{> message}} 

  <div class="row">
    <!-- Add new file card -->
    <div class="col-md-4 mb-4">
      <div class="card">
        <div class="card-body text-center">
          <h5 class="card-title">Add New File</h5>
          <form action="/upload" method="POST" enctype="multipart/form-data">
            <div class="form-group">
              <input type="file" name="file" class="form-control-file mb-2" required>
            </div>
            <div class="form-group">
              <label for="visibility">Visibility</label>
              <select name="visibility" class="form-control" required>
                <option value="self">Self</option>
                <option value="team">Team</option>
              </select>
            </div>
            <button type="submit" class="btn btn-success">Upload</button>
          </form>
        </div>
      </div>
    </div>

    <!-- Dynamically render uploaded files -->
    {{#each uploadedFiles}}
    <div class="col-md-4 mb-4">
      <div class="card">
        <div class="card-body text-center">
          <h5 class="card-title">{{file_name}}</h5>
          
          <!-- Display additional file details -->
          <p class="card-text">Visibility: {{visibility}}</p> <!-- Display file visibility -->
          <p class="card-text">Uploaded by: {{uploader_id}}</p> <!-- Assuming `uploader_id` is available -->

          <!-- Display download link -->
          <div>
            <a href="/files/{{file_name}}" class="btn btn-primary mb-2">Download</a>
            <a href="/edit-file/{{file_id}}" class="btn btn-secondary mb-2">Edit</a>
            <!-- Delete button -->
            <form action="/delete-file/{{file_id}}" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-danger">Delete</button>
            </form>

          </div>
        </div>
      </div>
    </div>
    {{/each}}
  </div>
</div>
